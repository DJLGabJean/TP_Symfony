{{ form_start(clothingform) }}
    {{ form_row(clothingform.name) }}
    {{ form_row(clothingform.categories) }}
    {{ form_row(clothingform.brand) }}
    {{ form_row(clothingform.sizes) }}
{{ form_end(clothingform) }}